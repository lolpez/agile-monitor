extends layout

block content
	.chart-container
		canvas#agile-graph
block scripts
	script.
		(function() {
			var agileChart = new AgileChart(document.getElementById('agile-graph'),	!{JSON.stringify(automators).replace(/<\//g, '<\\/')})
			agileChart.addRandomData();
		})();